# Editor
.obsidian/

# M Code Desktop runtime (логи сессий)
.mcode/

# OS
.DS_Store
Thumbs.db

# OpenCode runtime
.opencode/node_modules/
.opencode/package-lock.json

# Python venv (direnv)
.venv/
venv/
__pycache__/
*.pyc

# Python build artifacts
*.egg-info/
*.egg-link
build/
dist/

# Telegram capture tool (runtime, не коммитить)
tools/telegram-capture/.env
tools/telegram-capture/__pycache__/
tools/telegram-capture/signals.json
tools/telegram-capture/signals-*.json
tools/telegram-capture/inbox-queue.jsonl

# Ecosystem observer (generated snapshots, не коммитить)
tools/ecosystem-map/generated/

# model-router бэкапы (точечная правка model: — runtime, не коммитить)
.model-router-backups/

# Telegram session (авторизация, не коммитить!)
*.session
*.session-journal

# Credentials (корень волта)
.env
tools/verify-cache/generated/
