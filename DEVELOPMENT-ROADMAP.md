---
type: Roadmap
title: DEVELOPMENT-ROADMAP
description: Полный план работ с приоритетами P0–P5 по доведению волта до рабочего состояния.
timestamp: 2026-06-29
---
# DEVELOPMENT-ROADMAP — План доведения волта до рабочего состояния

> Приоритеты: P0 (критические баги) → P1 (структура) → P2 (наполнение) → P3 (полировка).
> Статусы: ❌ не начато · 🟡 в работе · ✅ готово · ➖ отменено

---

## P0 — Критические баги и противоречия

Всё, что ломает концепт или мешает работе librarian.

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 1 | ~~99-Inbox.md.md~~ → **99-Inbox.md** | ✅ | Файл переименован |
| 2 | **Убрать claude-mem** из всей базы | ✅ | Заменён на OKF-память 04-Memory/ |
| 3 | **Создать 04-Memory/** — OKF-подбандл памяти | ✅ | index, log, active-context, facts, session-log |
| 4 | **Исправить external_directory** librarian (ask → allow) + расширить bash whitelist | ✅ | Обновлён librarian.md |
| 5 | **Заполнить 02-Methods/** — 6 пустых файлов | ✅ | 228 строк, все 6 файлов непусты |

## P1 — Структурные недоработки

То, что держит каркас волта.

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 6 | **Применить OKF v0.1** ко всему волту | ✅ | index.md + log.md + frontmatter на все файлы |
| 7 | **Обновить 00-INDEX.md** — под OKF | ✅ | Добавлен type: Dashboard |
| 8 | **Обновить AGENTS.md** — убрать claude-mem, вписать новую память | ✅ | Обновлён |
| 9 | **Обновить Architecture.md** — OKF-структура | ✅ | Отражает OKF-бандл |
| 10 | **Наполнить rules-AGENTS.md** | ✅ | Описан формат AGENTS.md |
| 11 | **Создать opencode.json** в корне волта (с $schema) | ✅ | default_agent: librarian, $schema, LSP |
| 12 | **Проверить консистентность ссылок** | ✅ | all wikilink valid, mixed formats unified |

## P2 — Reference (справочник OpenCode)

Чего не хватает в 01-Reference.

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 12 | **memory.md** — наполнить под новую систему | ✅ | Готово |
| 13 | **OpenCode Zen** провайдер в config.md | ✅ | Полный раздел: подключение, модели, конфиг |
| 14 | **doom_loop, budget, steps** — cost control | ✅ | Полный раздел: steps, doom_loop, budgetTokens с примерами |
| 15 | **skills** — отдельный инструмент OpenCode | ✅ | Описан в permissions.md: механизм, SKILL.md, контроль доступа |
| 16 | **Plugin SDK** (@opencode-ai/plugin) | ✅ | plugins.md: SDK, events, custom tools, хуки проектов |

## P3 — Проекты и карточки

Актуализация карточек проектов.

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 17 | **dv-hub** — обновить статусы методов | ✅ | Убрано claude-mem, обновлён memory-management; все 6 методов ❌ |
| 18 | **SERPlux** — команды и статусы методов | ✅ | Команд нет (подтверждено); 6 методов: context-as-docs 🟡, остальное ❌ |
| 19 | **dotfiles** — обновлять после создания репо | ➖ | Репо создан, OpenCode не инициализирован |
| 20 | **vault** — обновить чеклист | ✅ | Чеклист синхронизирован с реальностью |
| 21 | **Создать единую таблицу** «Статус методов × Проекты» | ✅ | В 00-INDEX.md |

## P4 — Автоматизация и CI волта

Чтобы волт жил сам.

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 22 | **Пре-коммит хук** на проверку пустых файлов | ✅ | Пустые .md не коммитятся |
| 23 | **Плагин валидации викилинков** | ✅ | Встроен в pre-commit hook |
| 24 | **Авто-архивация session-log** раз в месяц | ✅ | 05-Templates/archive-session-log.sh |

---

## P5 — Будущая архитектура

Централизованное управление проектами через волт.

```mermaid
flowchart LR
    A[Волт / librarian] --> B[(Telegram-бот)]
    A --> C[/audit]
    A --> D[/project-add]
    A --> E[/project-upgrade]
    B --> F[Фичи и подходы из вне]
    F --> A
    C --> G[Сводка по всем проектам]
    E --> H[Авто-внедрение методов]
```

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 25 | **Команда `/audit`** — пакетный обход проектов | ✅ | git pull + сверка карточки + сводная таблица |
| 26 | **Telegram-бот** — присылает фичи, librarian классифицирует | ❌ | Фичи → какой проект? нужен апгрейд? |
| 27 | **Классификация фич по проектам** — автомат | ❌ | Бот принёс → librarian решил куда |
| 28 | **Команда `/project-upgrade`** — применить метод к проекту | ❌ | В будущем — авто-внедрение |
| 29 | **Сводка состояния проектов на дашборд** | ✅ | В 00-INDEX.md + ссылка на `/audit` |

## P6 — Порт органов M Code Desktop в TUI-стек

Решение Rudra (2026-09-05): **TUI — основное окружение**; M Code Desktop —
майнинг-станция фич (апдейтер догоняет апстрим). Новые органы движка
(ring of peers, task-фан-out с worktree-изоляцией, verify-кэш, Playwright-браузер)
изучены и документированы в [[01-Reference/mcode-desktop]] — очередь ниже =
порт семантики в TUI-стек.

| # | Задача | Статус | Примечание |
|---|--------|--------|-----------|
| 30 | **Reference-карточка M Code Desktop** — инвентаризация органов + статус миграции | ✅ | 2026-09-05: [[01-Reference/mcode-desktop]] |
| 42 | **Replay budget в TUI — ПЕРВЫЙ приоритет P6** — секрет экономии M Code (сессия за центы): старые tool-результаты при отправке истории режутся до 2000 симв. (head 75%/tail 25%), защита последних 40 KB, pruning старых tool-входов >120 симв., дроп неподписанного reasoning, image budget 5. В TUI 1.18.5 НЕТ (strings-проверка). Константы verified в app.asar: [[01-Reference/mcode-desktop]] § «Секрет экономии токенов» | ❌ | Патч TUI/prompt-пайплайна: truncateToolOutput + replayCapPlan по образцу M Code |
| 43 | **Doom-loop + no-op guard в TUI** — byte-identical tool-call ≥3 подряд → стоп turn с педагогическим текстом (THRESHOLD=3/BURST=9); пустой turn (<200 output tokens, без тула/ответа) → нудж. Константы verified | ❌ | Второй по ценности порт: убивает главные утечки токенов |
| 44 | **Auto-compaction + continuation summary** — threshold 100K tokens, summary ≤4096 output tokens, replay только summary+хвост (filterCompacted). Проверить, что в TUI 1.18.5 уже есть [проверить: у TUI compaction есть, сверить семантику] | ❌ | Возможно, частично уже есть в TUI |
| 45 | **Secret redaction service** — redactWith на replay частей из БД + PRUNED_INPUT_KEEP whitelist {path, command, pattern, url, offset, limit…} | ❌ | Security: секреты не попадают в контекст, нет leak-ретраев |
| 46 | **Санитизация ввода** — экранирование system-reminder/transport-markup из вставленного текста (SYSTEM_REMINDER_RE, TRANSPORT_MARKUP) | ❌ | Prompt-injection defense |
| 47 | **Per-session учёт cache_read токенов** — tokens_cache_read в статистике сессии | ❌ | Прозрачность расхода (как «проверил центы») |
| 40 | **Playwright-браузер в TUI** — браузерный тул (JS-рендеринг, скриншоты, DOM-извлечение); в M Code это `playwright-core`; усилит `/capture` и веб-аудиты | ❌ | TUI-webfetch без JS; проверено 2026-09-05 в app.asar.unpacked |
| 31 | **Peers-семантика в TUI** — claim/letter/hop-limits между параллельными TUI-сессиями (файл-реестр + протокол в capability-routing) | ❌ | Референс: peers/peer_role/peer_message из M Code |
| 32 | **Параллельный `/audit`** — fan-out task(explore) по проектам, сводка на дашборд | ❌ | Заменяет последовательный git pull-обход (T-014/T-25) |
| 33 | **verify-кэш по tree-hash** — машиностроимые гейты волта (пустые файлы, викилинки) не перезапускать на неизменённом дереве | ❌ | Дополняет pre-commit hook (T-011/T-012) |
| 34 | **verify-subagent acceptance** — независимый verifier по diff/evidence как стандарт acceptance | ❌ | Усиливает verifier-pattern, закрывает самодекларацию |
| 35 | **Decision queue на permission events** — сверить `permission.ask` payload плагина T-132, включить live hook fire | ❌ | Резольвит residuals T-132; после подтверждения → facts.md |
| 37 | **Oracle route** — вторая (более сильная) модель для дирижёрских решений через [[02-Methods/model-routing]] | ❌ | Референс: oracle break-glass семантика |
| 38 | **session-flush runtime** — подтвердить session.idle/compaction события для session-flush плагина | ❌ | Плагин есть, runtime-поведение не подтверждено [проверить] |
| 39 | **Медиа-канал отчётов (минимальный)** — артефакты сессий (сводки, карты) доставлять файлом + открытие через xdg-open | ❌ | TUI ограничен в медиа; референс: send_file/send_media |
| 41 | **Майнинг релизов M Code** — при выходе новых версий просматривать чейнджлог: новые органы → кандидаты для порта в P6 | ❌ | Апдейтер channel: latest; периодическая задача, не блокер |
| 36 | **Memory-стор — оценить** | ➖ | Решение: 04-Memory остаётся SSOT; отдельный стор движка не нужен (файловый слой уже живёт) |

## Легенда
- **P0** — блокеры: без них волт не работает как задумано
- **P1** — структура: каркас, на который навешивается контент
- **P2** — наполнение Reference: чтобы не гадать, а читать
- **P3** — проекты: живые карточки — главная ценность для пользователя
- **P4** — автоматизация: чтобы не обслуживать волт руками
- **P5** — будущее: Telegram-бот, авто-апгрейды, централизованное управление
- **P6** — порт органов M Code Desktop в TUI-стек ([[01-Reference/mcode-desktop]])

## Как обновлять
Нашёл новую задачу → добавь строку в соответствующую таблицу.
Выполнил → смени статус на ✅ и дату.
Отменил → ➖ с причиной.
