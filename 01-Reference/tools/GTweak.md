---
type: Reference
title: GTweak — Windows-твикер (сторонний)
description: Портабельный Windows-твикер Greedeks/GTweak. Гайд по применению на чужой машине с риск-классами операций и чек-листом отката.
tags: [tools, windows, tweak, third-party]
timestamp: 2026-07-14
---
# GTweak — Windows-твикер

> Выжимка из официального README-ru: https://github.com/Greedeks/GTweak/blob/main/README-ru.md
> Проверено: 2026-07-13. Версия: 5.5.2 (выпущена 18.06.2026).

## Сводка

**GTweak** — сторонний портабельный твикер Windows на C#/.NET 4.8/WPF (BSD-3-Clause). Не проект Rudra; используется **редко**, на **чужом ноутбуке**. Ключевое ограничение: акцент на безопасность и обратимость — операции поделены на риск-классы 🟢/🟡/🔴, на чужой машине 🔴 не выполняются без явной инструкции владельца.

- **Версия:** 5.5.2 (18.06.2026)
- **Лицензия:** BSD-3-Clause
- **Язык:** C#/.NET Framework 4.8 / WPF (есть русская локализация — README-ru)
- **Портабельность:** `gtweak.exe` — single-file, installation не требуется
- **ОС:** Windows 10 build 18362.116+ (включая 11)
- **Источник:** https://github.com/Greedeks/GTweak/blob/main/README-ru.md (проверено 2026-07-13)

## Быстрый старт с нуля

> Пошаговая инструкция: от скачивания до возврата машины владельцу. Для тех, кто видит GTweak впервые.

### Шаг 1. Скачивание (на своей машине)

1. Открой в браузере: **https://github.com/Greedeks/GTweak/releases/latest**
2. Нажми на файл **`gtweak.exe`** в списке Assets (или прямая ссылка: https://github.com/Greedeks/GTweak/releases/latest/download/gtweak.exe)
3. Файл ~10-20 МБ, скачается в `Downloads/`

**Важно:** качай ТОЛЬКО из официального репо `Greedeks/GTweak`. Никаких сторонних сайтов.

### Шаг 2. Запись на флешку

GTweak — **портабельный** (single-file `.exe`), установка НЕ нужна. Просто скопируй файл:

1. Вставь флешку (любую, хоть 1 ГБ)
2. Скопируй `gtweak.exe` из `Downloads/` в корень флешки (или в любую папку)
3. **Всё.** Флешка готова — можно идти на чужую машину

**Альтернатива:** если не нужна флешка, можно просто скопировать `.exe` на рабочий стол целевой машины через облако/мессенджер/локальную сеть.

### Шаг 3. Подготовка целевой машины (чужой ноутбук)

Перед запуском:

1. **Создай точку восстановления системы:**
   - Нажми `Win + R` → введи `SystemPropertiesProtection` → Enter
   - Выбери системный диск (обычно C:) → "Настроить" → "Включить защиту" → OK
   - Нажми "Создать..." → дай имя (например, "До GTweak") → Создать
   - **Это обязательно** перед любыми твиками 🟡/🔴

2. **Добавь `gtweak.exe` в исключения Defender** (иначе антивирус может ругаться):
   - `Пуск` → `Безопасность Windows` → `Защита от вирусов и угроз` → `Управление настройками`
   - Прокрути вниз → `Исключения` → `Добавить или удалить исключения` → `Добавить исключение` → `Файл`
   - Укажи путь к `gtweak.exe` на флешке

### Шаг 4. Запуск

1. Открой флешку в Проводнике
2. **Правый клик на `gtweak.exe`** → **"Запуск от имени администратора"**
3. Если Windows спросит "Разрешить этому приложению вносить изменения?" → **Да**
4. Интерфейс на русском (есть локализация)

### Шаг 5. Что делать в интерфейсе

Интерфейс разделён на **8 секций** (слева):

| Секция | Что делает | Риск |
|--------|-----------|------|
| **Утилиты** | Чистка темпов, кэша, RAM, мониторинг железа | 🟢 Безопасно |
| **Конфиденциальность** | Телеметрия off, кейлоггеры off, блокировка доменов | 🟢-🟡 |
| **Интерфейс** | Темы, панель задач, иконки, клавиатура/мышь | 🟢 Безопасно |
| **Приложения** | Удаление Edge, OneDrive, UWP, Cortana/Copilot | 🔴 Необратимо |
| **Службы** | Отключение системных служб, планировщика | 🟡-🟢 |
| **Система** | Обновления Windows, Defender, UAC, питание | 🟡 Средний |
| **Расширения** | NTFS-сжатие, доп. функции | 🟡 |
| **Набор инструментов** | Запуск скриптов с правами TrustedInstaller | 🔴 Только для экспертов |

**Правило для чужой машины:**
- 🟢 — применяй свободно
- 🟡 — только с согласия владельца
- 🔴 — НЕ трогай без явной письменной инструкции владельца

### Шаг 6. Применение твиков

В каждой секции — переключатели/кнопки. Включай что нужно, затем нажми **"Применить"** (обычно внизу или вверху секции).

**Рекомендуемый минимум для чужой машины (🟢):**
- Утилиты → Чистка временных файлов
- Конфиденциальность → Отключить телеметрию Windows
- Интерфейс → любые UI-твики по вкусу

### Шаг 7. После применения

1. **Перезагрузи ноутбук**
2. Проверь что всё работает: браузер, Wi-Fi, звук, почта
3. Если что-то сломалось → используй точку восстановления (Шаг 3.1):
   - `Win + R` → `rstrui` → Enter → "Далее" → выбери точку "До GTweak" → "Далее" → "Готово"

### Шаг 8. Возврат владельцу

Оставь владельцу:
- Список применённых твиков (скриншот или текст)
- Инструкцию как откатить (через точку восстановления)
- Флешку с `gtweak.exe` (если владелец хочет пользоваться сам)

## Системные требования и загрузка

- **Windows:** 10 build 18362.116+ или 11
- **.NET Framework:** 4.8 (предустановлен на Win10 2004+/Win11)
- **Скачать (прямая ссылка на latest release):** https://github.com/Greedeks/GTweak/releases/latest/download/gtweak.exe
- **Подлинность:** качать ТОЛЬКО из официального репо `Greedeks/GTweak` (releases). `gtweak.exe` портабельный — установка не нужна, запуск с диска/флешки.
- **Текущая версия:** 5.5.2 от 18.06.2026 (https://github.com/Greedeks/GTweak/releases/latest)

## Группы операций по риску (для чужой машины)

> На чужой машине — применять в первую очередь 🟢, 🟡 по согласию, 🔴 не трогать без явной инструкции владельца.

### 🟢 Безопасно / обратимо

UI-твики, телеметрия off, чистка темпов/кэша, отключение «теневых» сервисов, мониторинг железа. Откат через реестр или возврат переключателя в GTweak.

| Категория | Примеры |
|-----------|---------|
| Кастом UI | темы, окна, панель задач, иконки |
| Телеметрия | выключение телеметрии Windows/NVIDIA/Intel, кейлоггеров |
| Чистка | RAM, темпы, кэш, Windows.old (только если не удалять системно — см. 🔴 для случаев removal) |
| Отключение сервисов | отключение «теневых» задач планировщика, Teredo/ISATAP/IPv6, дефрагментация, реклама/SCOOBE, уведомления |
| Мониторинг | мониторинг железа |
| Периферия | настройки клавы/мыши (фильтрация/залипание/акселерация) |

### 🟡 Средний риск / осторожно

Откат возможен, но требует времени/сноровки. **На чужой машине — получить согласие владельца; создать точку восстановления Windows перед.**

- Отключение обновлений Windows (пауза) + чистка темпов
- Отключение Defender / SmartScreen / Antimalware / VBS / UAC
- Power plan «Максимальная производительность»
- NTFS-сжатие
- Блокировка «теневых» доменов Microsoft через hosts+firewall
- Realtek Audio тайминги

### 🔴 Необратимо / откат без переустановки труден

**На чужой машине — НЕ ВЫПОЛНЯТЬ без явной инструкции владельца.**

- Удаление Edge (+ WebView2 + связанные данные)
- Удаление OneDrive (+ связанные данные)
- Удаление UWP-приложений
- Удаление Cortana/Copilot/Recall
- Удаление Windows.old (системно — если требуется clean rollback)
- **Активация Windows (HWID/KMS):** HWID привязывается к железу; KMS — 180 дней. **Если Windows уже активирована — НЕ трогать.**
- Запуск сторонних `.ps1/.cmd/.bat/.reg` с правами TrustedInstaller (SYSTEM-уровень)

> Откат Edge/OneDrive требует ручных шагов (reinstall) или `sfc`/`DISM`-восстановления. См. секцию «Откат».

## Пошаговый чек-лист для чужой машины

1. **Создать точку восстановления системы** — `SystemPropertiesProtection` → Create. Обязательно перед 🟡/🔴.
2. **Резервная копия** профиля владельца / важных данных (опционально — если есть доступ к диску/образу).
3. **Убедиться, что Windows официального образа** (не обрезанная/изменённая). См. официальное предупреждение ниже.
4. **Отключить антивирус / добавить `gtweak.exe` в исключения Defender** (согласно warning из README-ru).
5. **Запустить `gtweak.exe` от администратора** — правый клик → «Запуск от имени администратора».
6. **Выбрать язык интерфейса** (RU — README-ru свидетельствует о локализации).
7. **Авторизоваться у владельца перед секциями 🔴** — получить явное «да» по каждому пункту.
8. **Применять:** 🟢 — свободно; 🟡 — по явному согласию; 🔴 — пропустить (или только по письменной инструкции).
9. **После применения — перезагрузка**, проверка не сломан ли workflow: браузер по умолчанию, почта, OneDrive (если использовался), сеть, антивирус.
10. **До возврата ноутбука** — оставить записку + бэкап точки восстановления + список применённых твиков владельцу.

## Что НЕ делать на чужой машине

- **Не применять HWID/KMS, если Windows уже активирована** — флаг «уже активирована» = не трогать; HWID привяжется к железу, KMS истечёт через 180 дней.
- **Не удалять Edge/OneDrive, если владелец их использует** — откат трудоёмкий (reinstall через `winget`/microsoft.com).
- **Не отключать Defender полностью** — лучше временная пауза; на чужой машине оставить защиту для владельца.
- **Не запускать сторонние `.ps1` из непроверенных источников с TrustedInstaller** — они получают SYSTEM-уровень доступа; только авторские скрипты из доверенного репо.
- **Не применять «Максимальная производительность» на ноутбуке без зарядки** — батарея разрядится быстрее.
- **Не блокировать Microsoft-домены через hosts**, если используются Office 365/OneDrive/Teams — сломает подключение.

## Откат / восстановление

- **Точка восстановления:** System Restore (создаётся на шаге 1 чек-листа).
- **Системные компоненты:**
  ```powershell
  sfc /scannow
  DISM /Online /Cleanup-Image /RestoreHealth
  ```
- **OneDrive reinstall:** `winget install Microsoft.OneDrive` или скачать с onedrive.com.
- **Edge reinstall:** скачать с microsoft.com/edge (или на чужой машине — не удалять вообще).
- **UWP возврат:**
  ```powershell
  Get-AppxPackage -AllUsers | Foreach {Add-AppxPackage -DisableDevelopmentMode -Register "$($_.InstallLocation)\AppXManifest.xml"}
  ```
  Или через Microsoft Store — переустановка вручную.
- **Defender обратно:**
  ```powershell
  Set-MpPreference -DisableRealtimeMonitoring $false
  ```

## Известные предостережения из README-ru

> «⚠ Утилита предназначена только для официальных образов Windows, загруженные из надежных источников. Если у вас установлена обрезанная/измененная версия Windows, за работоспособность отвечаете вы сами.»
> — README-ru

> «Перед использованием остановите антивирус. Если вы используете Windows Defender, дополнительно добавьте её в исключения.»
> — README-ru (Warning)

## Секции интерфейса GTweak

По скриншотам README-ru: **Утилиты · Конфиденциальность · Интерфейс · Приложения · Службы · Система · Расширения · Набор инструментов**.

## Ссылки

- **Репозиторий:** https://github.com/Greedeks/GTweak
- **README-ru (главный источник):** https://github.com/Greedeks/GTweak/blob/main/README-ru.md
- **Releases (latest):** https://github.com/Greedeks/GTweak/releases/latest
- **Прямой download** `gtweak.exe`: https://github.com/Greedeks/GTweak/releases/latest/download/gtweak.exe
- **Лицензия:** BSD-3-Clause (см. `LICENSE` в репо)
- **NuGet-зависимости:** перечислены в `packages.config`/`.csproj` репо — см. репо для деталей
- **Автор:** Greedeks на GitHub (репорт багов через Issues; README-ru также упоминает Telegram-контакт — см. README для актуальной ссылки)