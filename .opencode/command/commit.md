
---
description: Commit vault changes with auto-generated conventional message. Минимум токенов.
agent: librarian
subtask: true
---
Посмотри `git status` и `git diff --stat`. Сформируй conventional commit message на русском. Выполни `git add` нужных файлов и `git commit`. Только commit, ничего лишнего.
